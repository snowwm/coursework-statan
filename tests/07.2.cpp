// !test bad_file_access
#include<iostream> // без уязвимости
#include <cstdio>

int main ()
{
  char *filename = “file.txt”;
  int acc = access(filename, 0);
  if (a == 0){
  FILE * ptrFile = fopen(filename, "w");

  if (ptrFile != NULL)
  {
fputs("Примериспользованияфункцииfopen ", ptrFile);
fclose (ptrFile);
          }}
          return 0;
        }